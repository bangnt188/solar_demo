export type Project = {
  title: string;
  category: string;
  location: string;
  description: string;
  system: string;
  image: string;
};

export type Equipment = {
  title: string;
  category: string;
  description: string;
  image: string;
};
